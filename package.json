{
  "scripts": {
    "build": "tailwindcss build -i input.css -o output.css --minify",
    "start-server": "http-server ./src/templates",
    "watch": "npx tailwindcss -i ./input.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.15",
    "http-server": "^14.1.1",
    "tailwindcss": "^3.3.3"
  }
}
